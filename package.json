{
  "name": "@adaptably/to",
  "type": "module",
  "version": "1.0.1",
  "description": "Get the absolute path to a file in an ES6 module.",
  "main": "source/to.js",
  "repository": {
    "type": "git",
    "url": "git@github.com:adaptably/to.git"
  },
  "scripts": {
    "test": "vitest run --globals"
  },
  "devDependencies": {
    "vitest": "^0.25.1"
  }
}
